{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}

<h1 id="quizTitle">//QUIZ_</h1>
<form id="quizComponent" name="quizForm" method="POST">
    {% for question in course.questions %}
        <div id="quizField{{question.questionNumber}}" class="quizField">
            <div id="progressBar"></div>
            <ul>
                <li id="questionList">
                    {{question.label}}
                </li>
            </ul>
            <div class="answersContainer">
                {% for answer in question.answers %}
                    <ul>
                        <li class="answersField">
                            <input type="checkbox" name="answersFiled" value="{{answer.isCorrect}}"/>
                            <label for="answersField">{{answer.label}}</label>
                        </li>
                    </ul>
                {% endfor %}
            </div>
            <div class ="buttonContainer">
                <button class="btn-glitch-fill">
                    <span class="decoration">&lArr;</span>
                    <span class="text-decoration">_</span>
                    <span class="text">//Retour</span>
                </button>
            </div>
        </div>
    {% endfor %}
</form>
{% endblock %}
