<form method="post" action="{{ path('playlist_delete', {'id': playlist.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette playlist ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ playlist.id) }}">
	<button class="btn" id="delete-playlist">Supprimer la playlist</button>
</form>
